<UserControl x:Class="PersonalAutomationTool.Modules.Database.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F6FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Esploratore Database SQLite" FontSize="28" FontWeight="SemiBold" Margin="20,20,20,10" Foreground="#2D3436"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,0,20,10">
            <Button Content="Ricarica DB" Margin="0,0,0,0" Padding="15,5" Click="BtnReload_Click" Background="#0984E3" Foreground="White" BorderThickness="0" Cursor="Hand" />
            
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="15,0,15,0" />
            
            <Button Content="Nuova Riga" Margin="0,0,5,0" Padding="15,5" Click="BtnAddRow_Click" Background="#27AE60" Foreground="White" BorderThickness="0" Cursor="Hand" />
            <Button Content="Elimina Riga" Margin="0,0,5,0" Padding="15,5" Click="BtnDeleteRow_Click" Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand" />
            <Button Content="Salva Modifiche" Margin="10,0,0,0" Padding="15,5" Click="BtnSaveChanges_Click" Background="#F39C12" Foreground="White" BorderThickness="0" Cursor="Hand" />
        </StackPanel>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Margin="20,10,20,20" Padding="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="1"/>
            </Border.Effect>
            <!-- DataGrid reso modificabile -->
            <DataGrid x:Name="MainDataGrid" 
                      AutoGenerateColumns="True" 
                      IsReadOnly="False" 
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CellEditEnding="MainDataGrid_CellEditEnding"
                      HeadersVisibility="Column" 
                      RowBackground="#FFF"
                      AlternatingRowBackground="#F9F9F9"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal" />
        </Border>
    </Grid>
</UserControl>
