<UserControl x:Class="PersonalAutomationTool.Modules.Email.EmailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="16" BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.15" BlurRadius="15" Direction="270" ShadowDepth="5"/>
                            </Border.Effect>
                            <Grid>
                                <!-- Highlight overlay per hover -->
                                <Border x:Name="HoverOverlay" Background="White" Opacity="0" CornerRadius="16"/>
                                <!-- Shadow overlay per pressed -->
                                <Border x:Name="PressedOverlay" Background="Black" Opacity="0" CornerRadius="16"/>
                                
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.2"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" Opacity="0.25" BlurRadius="20" Direction="270" ShadowDepth="8"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="-2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PressedOverlay" Property="Opacity" Value="0.2"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="5" Direction="270" ShadowDepth="2"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="24"/>
            <Setter Property="Margin" Value="40"/>
            <Setter Property="Padding" Value="40"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#D0D6E0" Opacity="0.8" BlurRadius="25" Direction="270" ShadowDepth="12"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Main Container with subtle gradient background -->
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F8FAFC" Offset="0.0" />
                <GradientStop Color="#EDF2F7" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Border Style="{StaticResource CardStyle}">
            <!-- The Viewbox ensures the inner content scales perfectly UP or DOWN depending on the window size -->
            <Viewbox Stretch="Uniform" StretchDirection="Both" Margin="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Row 0 -->
                    <Button Grid.Row="0" Grid.Column="0" Content="E404P" Style="{StaticResource ModernButtonStyle}" Background="#F56565" Click="Nav_E404P"/>
                    <Button Grid.Row="0" Grid.Column="1" Content="ETR700" Style="{StaticResource ModernButtonStyle}" Background="#38B2AC" Click="Nav_ETR700"/>
                    <Button Grid.Row="0" Grid.Column="2" Content="ETR1000" Style="{StaticResource ModernButtonStyle}" Background="#4299E1" Click="Nav_ETR1000"/>
                    <Button Grid.Row="0" Grid.Column="3" Content="ETR1000 IF" Style="{StaticResource ModernButtonStyle}" Background="#667EEA" Click="Nav_ETR1000IF"/>
                    
                    <!-- Row 1 -->
                    <Button Grid.Row="1" Grid.Column="0" Content="ETR1000FH" Style="{StaticResource ModernButtonStyle}" Background="#ED8936" Click="Nav_ETR1000FH"/>
                    <Button Grid.Row="1" Grid.Column="1" Content="ETR421" Style="{StaticResource ModernButtonStyle}" Background="#48BB78" Click="Nav_ETR421"/>
                    <Button Grid.Row="1" Grid.Column="2" Content="ETR521" Style="{StaticResource ModernButtonStyle}" Background="#9F7AEA" Click="Nav_ETR521"/>
                    <Button Grid.Row="1" Grid.Column="3" Content="ETR522" Style="{StaticResource ModernButtonStyle}" Background="#ECC94B" Click="Nav_ETR522"/>
                </Grid>
            </Viewbox>
        </Border>
    </Grid>
</UserControl>
