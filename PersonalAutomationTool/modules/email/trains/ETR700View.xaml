<UserControl x:Class="PersonalAutomationTool.Modules.Email.Trains.ETR700View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header con tasto indietro -->
        <Grid Margin="20" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="← Indietro" Width="100" Height="40" Click="BtnBack_Click" 
                    Background="Transparent" BorderBrush="#E2E8F0" BorderThickness="1" 
                    Foreground="#34495E" FontSize="14" FontWeight="SemiBold" Cursor="Hand"/>
            <TextBlock Grid.Column="1" Text="Generazione Email ETR700" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="⚙ Destinatari" Width="120" Height="40" Click="BtnSettings_Click"
                    Background="#34495E" Foreground="White" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <!-- Contenuto principale -->
        <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="16" Margin="20,0,20,20" Padding="20">
            <TextBlock Text="Work in progress per ETR700..." FontSize="16" Foreground="#7F8C8D" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
