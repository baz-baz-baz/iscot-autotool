<UserControl x:Class="PersonalAutomationTool.Modules.Email.Trains.ETR521View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header con tasto indietro -->
        <Grid Margin="20" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Indietro" Width="100" Height="40" Click="BtnBack_Click" 
                    Background="Transparent" BorderBrush="#E2E8F0" BorderThickness="1" 
                    Foreground="#34495E" FontSize="14" FontWeight="SemiBold" Cursor="Hand"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Generazione Email ETR521" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                <TextBlock Text="Cartella selezionata:" FontSize="14" Foreground="#7F8C8D" VerticalAlignment="Center" Margin="40,0,10,0" FontWeight="SemiBold"/>
                <ComboBox x:Name="CmbCartelle" Width="250" Height="35" FontSize="14" VerticalContentAlignment="Center" Padding="10,0"/>
            </StackPanel>
        </Grid>

        <!-- Contenuto principale -->
        <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="16" Margin="20,0,20,20" Padding="20">
            <TextBlock Text="Work in progress per ETR521..." FontSize="16" Foreground="#7F8C8D" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
