<UserControl x:Class="PersonalAutomationTool.Modules.Email.Trains.E404PView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header con tasto indietro -->
        <Grid Margin="20" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Indietro" Width="100" Height="40" Click="BtnBack_Click" 
                    Background="Transparent" BorderBrush="#E2E8F0" BorderThickness="1" 
                    Foreground="#34495E" FontSize="14" FontWeight="SemiBold" Cursor="Hand"/>
            <TextBlock Grid.Column="1" Text="Generazione Email E404P" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="20,0,0,0" VerticalAlignment="Center"/>
            
        </Grid>

        <!-- Contenuto principale -->
        <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="16" Margin="20,0,20,20" Padding="20">
            <Border.Resources>
                <Style TargetType="Button" x:Key="EmailActionBtn">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="Height" Value="80"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="10" Direction="270" ShadowDepth="4"/>
                                    </Border.Effect>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F1F5F9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#E2E8F0"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Resources>
            
            <StackPanel Margin="20">
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Seleziona Cartella:" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5" FontSize="16"/>
                    <ComboBox x:Name="CmbCartelle" Height="40" FontSize="14" VerticalContentAlignment="Center" Padding="10,0"/>
                </StackPanel>
                <Grid VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Row 0 -->
                <Button Grid.Row="0" Grid.Column="0" Content="Chiusura Ticket" Style="{StaticResource EmailActionBtn}" Click="BtnChiusuraTicket_Click"/>
                <Button Grid.Row="0" Grid.Column="1" Content="Log Dump" Style="{StaticResource EmailActionBtn}" Click="BtnLogDump_Click"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Scadenza 6 Mesi" Style="{StaticResource EmailActionBtn}" Click="BtnScadenza6Mesi_Click"/>

                <!-- Row 1 -->
                <Button Grid.Row="1" Grid.Column="0" Content="Scadenza 12 Mesi" Style="{StaticResource EmailActionBtn}" Click="BtnScadenza12Mesi_Click"/>
                <Button Grid.Row="1" Grid.Column="1" Content="Scadenza V.I" Style="{StaticResource EmailActionBtn}" Click="BtnScadenzaVI_Click"/>
                <Button Grid.Row="1" Grid.Column="2" Content="Scadenza V.T" Style="{StaticResource EmailActionBtn}" Click="BtnScadenzaVT_Click"/>

                <!-- Row 2 -->
                <Button Grid.Row="2" Grid.Column="0" Content="R2" Style="{StaticResource EmailActionBtn}" Click="BtnR2_Click"/>
            </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
